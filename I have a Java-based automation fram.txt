I am building a scalable test execution platform for a Java-based automation framework (Cucumber + TestNG).

Current setup:
- Tests run via a main class
- Reports generated in local HTML format
- Jenkins job already exists with parameters (e.g., browser)

Requirements:
- React UI to trigger executions
- Spring Boot backend as orchestration layer
- Each execution should have a unique runId
- Execution must be asynchronous (non-blocking APIs)
- Users should see execution status and history
- Reports should be accessible via URL
- System should support scaling (parallel runs, multiple users, env selection)

Design a production-grade architecture including:
1. API contract (request/response structure)
2. Execution orchestration strategy (Async vs Queue vs Jenkins)
3. Best way to integrate Jenkins via API
4. Report storage strategy (local vs cloud like S3)
5. Database schema for execution tracking
6. Handling parallel execution and scaling
7. Failure handling and retry strategy
8. Security considerations (auth, access control)
9. CI/CD integration approach
10. Step-by-step phased implementation plan

Also include sample code snippets for:
- Async execution in Spring Boot
- Jenkins job trigger via REST API
- Generating unique runId
